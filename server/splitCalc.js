// server/splitCalc.js — thin wrapper around @splitdumb/core
const { calculateSplits, buildNightsData, CATEGORIES } = require('@splitdumb/core');
module.exports = { calculateSplits, buildNightsData, CATEGORIES };